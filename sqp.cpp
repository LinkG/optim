#include<iostream>
#include<vector>
#include<functional>

using namespace std;

//Takes a function 'f' and computes the unconstrained argmin of f
float lagrange_newton(function<float(float)> f) {

    return 0;
}

//Takes a function 'f' as argument and hopes to compute argmin of f subject to
//conditions g_i and h_i.
float sqp(function<float(float)> f, function<float(float)>* g, function<float(float)>* h) {

    return 0;
}

int main() {
    
    return 0;
}